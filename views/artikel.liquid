{{ file }}

<style>
  body {
    background-color: #0e0d0d;
    color: #f0f0f0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 1rem;
    line-height: 1.7;
    padding: 2rem;
    margin: auto;
    max-width: 56.25rem;

  }

  img {
    width: 100%;
    max-width: 20rem;
    height: auto;
    object-fit: contain;
    border-radius: 0.5rem;
    transition: transform 0.3s ease;
    box-shadow: 0 0.3rem 0.75rem rgba(0, 0, 0, 0.4);
    display: block;
    margin: 1rem auto;
  }


  img:hover {
    transform: scale(1.03);
    box-shadow: 0 0.6rem 1rem rgba(239, 90, 175, 0.5);
  }

  h1 {

    color: rgb(239, 90, 175);
  }
  h2 {
    color: #ffe6d6;
  }


  a {
    color: #f49ac2;
    text-decoration: none;
    font-weight: bold;
    display: inline-block;
    animation: soft-heartbeat 3s infinite ease-in-out;
    transition: color 0.3s ease;
  }

  @keyframes soft-heartbeat {
    0% {
      transform: scale(1);
    }
    30% {
      transform: scale(1.05);
    }
    50% {
      transform: scale(1);
    }
    80% {
      transform: scale(1.05);
    }
    100% {
      transform: scale(1);
    }
  }


  hr {
    border: none;
    border-top: 1px solid #444;
    margin: 3rem 0;
  }
</style>